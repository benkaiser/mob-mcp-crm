<%
const content = `
  ${ error ? '<div class="error">' + error + '</div>' : '' }
  ${ success ? '<div class="success">' + success + '</div>' : '' }
  <p class="alt-link"><a href="/app/">Go to the app</a></p>
  ${ error ? '<p class="alt-link" style="font-size:0.9rem;">You can request a new link from Settings after signing in.</p>' : '' }
`;
%>
<%- include('_auth-layout', { title: 'Email verification - Mob CRM', subtitle: 'Email verification', content }) %>
